# Assets Directory

Place your image assets in this directory.

## Recommended Files
- `hero-wireframe.png` (For the Hero section)
- `platform-antarleen.jpg` (For the Platform section background)
- `noise.svg` (For background texture)

Once you have added the files, update `constants/assets.ts` to import and use them instead of the remote URLs.